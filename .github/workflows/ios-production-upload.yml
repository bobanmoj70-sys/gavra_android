name: iOS Production Upload

"on": [workflow_dispatch]

jobs:
  build-and-upload:
    runs-on: macos-latest
    timeout-minutes: 90
    env:
      BUNDLE_ID: com.gavra013.gavra013ios

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install iOS signing tools
        run: pip3 install codemagic-cli-tools

      - name: Ensure .env exists for Flutter assets
        shell: bash
        run: |
          if [ -n "${{ secrets.ENV_FILE_CONTENT }}" ]; then
            printf "%s" "${{ secrets.ENV_FILE_CONTENT }}" > .env
          elif [ -n "${{ secrets.IOS_ENV }}" ]; then
            printf "%s" "${{ secrets.IOS_ENV }}" > .env
          else
            touch .env
          fi

      - name: Prepare App Store Connect keys
        shell: bash
        env:
          APP_STORE_CONNECT_KEY_IDENTIFIER: ${{ secrets.APP_STORE_CONNECT_KEY_IDENTIFIER }}
        run: |
          mkdir -p ~/.private_keys

          if [ -n "${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY_BASE64 }}" ]; then
            printf "%s" "${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY_BASE64 }}" | base64 -d > ~/.private_keys/AuthKey_${APP_STORE_CONNECT_KEY_IDENTIFIER}.p8
          elif [ -n "${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY }}" ]; then
            printf "%s" "${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY }}" > ~/.private_keys/AuthKey_${APP_STORE_CONNECT_KEY_IDENTIFIER}.p8
          else
            echo "Missing App Store Connect private key secret."
            exit 1
          fi

          if [ -n "${{ secrets.CERTIFICATE_PRIVATE_KEY_BASE64 }}" ]; then
            printf "%s" "${{ secrets.CERTIFICATE_PRIVATE_KEY_BASE64 }}" | base64 -d > /tmp/certificate_private_key.pem
          elif [ -n "${{ secrets.CERTIFICATE_PRIVATE_KEY }}" ]; then
            printf "%s" "${{ secrets.CERTIFICATE_PRIVATE_KEY }}" > /tmp/certificate_private_key.pem
          fi

      - name: Flutter dependencies
        run: flutter pub get

      - name: Install CocoaPods
        run: |
          cd ios
          pod install --repo-update

      - name: Setup keychain
        run: keychain initialize

      - name: Fetch signing files
        env:
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_KEY_IDENTIFIER: ${{ secrets.APP_STORE_CONNECT_KEY_IDENTIFIER }}
          APP_STORE_CONNECT_PRIVATE_KEY: "@file:${HOME}/.private_keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_IDENTIFIER }}.p8"
          CERTIFICATE_PRIVATE_KEY: "@file:/tmp/certificate_private_key.pem"
        run: |
          app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_STORE --create

      - name: Add certificates to keychain
        run: keychain add-certificates

      - name: Apply provisioning profiles
        run: xcode-project use-profiles

      - name: Build IPA
        run: flutter build ipa --release --export-options-plist=$HOME/export_options.plist

      - name: Install Fastlane dependencies
        run: |
          cd ios
          bundle install

      - name: Resolve IPA path
        shell: bash
        run: |
          IPA_PATH=$(find build -name '*.ipa' | head -1)
          if [ -z "$IPA_PATH" ]; then
            echo "IPA file not found"
            exit 1
          fi
          echo "IPA_PATH=$(pwd)/$IPA_PATH" >> $GITHUB_ENV

      - name: Prepare API key JSON
        shell: bash
        env:
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_KEY_IDENTIFIER: ${{ secrets.APP_STORE_CONNECT_KEY_IDENTIFIER }}
        run: |
          KEY_PATH="$HOME/.private_keys/AuthKey_${APP_STORE_CONNECT_KEY_IDENTIFIER}.p8"
          python3 - <<'PY'
import json, os
payload = {
  "key_id": os.environ["APP_STORE_CONNECT_KEY_IDENTIFIER"],
  "issuer_id": os.environ["APP_STORE_CONNECT_ISSUER_ID"],
  "key": open(os.path.expanduser(f"~/.private_keys/AuthKey_{os.environ['APP_STORE_CONNECT_KEY_IDENTIFIER']}.p8"), encoding="utf-8").read(),
  "in_house": False,
}
open("/tmp/asc_api_key.json", "w", encoding="utf-8").write(json.dumps(payload))
PY
          echo "ASC_API_KEY_JSON=/tmp/asc_api_key.json" >> $GITHUB_ENV

      - name: Upload to TestFlight (production flow)
        run: |
          cd ios
          bundle exec fastlane pilot upload \
            --ipa "$IPA_PATH" \
            --api_key_path "$ASC_API_KEY_JSON" \
            --skip_waiting_for_build_processing false \
            --distribute_external false \
            --changelog "Automated production upload."
